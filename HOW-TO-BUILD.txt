================================================================================
              كيفية بناء وتحميل تطبيق Android v1.0.2
           (إصلاح مشكلة عدم ظهور حقول تسجيل الدخول)
================================================================================

📝 التحديثات في v1.0.2:
----------------------------------------
✅ إصلاح عرض حقول تسجيل الدخول (fillColor: white بدلاً من gray)
✅ تحسين borders والتباين (width: 1.5px → 2.5px)
✅ زيادة وضوح النص (fontWeight: w500)
✅ تحسين الأيقونات والـ padding


📦 خيارات البناء:
================================================================================

الخيار 1: بناء تلقائي عبر GitHub Actions (موصى به)
-------------------------------------------------------
✅ سريع وسهل
✅ لا يحتاج Flutter محلياً
✅ ينتج 4 ملفات APK (universal + 3 architectures)

الخطوات:
1. ارفع المجلد flutter_attendance_app إلى GitHub
2. الـ workflow سيبدأ تلقائياً
3. بعد 5-7 دقائق، حمّل APK من قسم "Artifacts"


الخيار 2: بناء يدوي محلياً
----------------------------
⚠️ يحتاج Flutter SDK مثبت

```bash
# 1. تنظيف
cd flutter_attendance_app
flutter clean

# 2. تحميل Dependencies
flutter pub get

# 3. بناء APK
flutter build apk --release

# 4. الملف في:
# build/app/outputs/flutter-apk/app-release.apk
```


الخيار 3: بناء عبر CI/CD (GitHub Actions)
------------------------------------------
الملف الموجود: .github/workflows/build-apk.yml

يبني 4 إصدارات:
- app-universal-release.apk (جميع الأجهزة) - 45 MB
- app-arm64-v8a-release.apk (أجهزة حديثة) - 20 MB
- app-armeabi-v7a-release.apk (أجهزة قديمة) - 18 MB
- app-x86_64-release.apk (محاكيات) - 22 MB


🚀 خطوات رفع إلى GitHub:
================================================================================

إذا لم يكن التطبيق على GitHub بعد:

```bash
cd flutter_attendance_app

# إنشاء مستودع جديد
git init
git add .
git commit -m "Initial commit - Attendance App v1.0.2"

# ربط بـ GitHub
git remote add origin https://github.com/YOUR_USERNAME/jabalia-attendance-app.git
git branch -M main
git push -u origin main
```

الـ workflow سيبدأ تلقائياً بعد الـ push!


📥 تحميل APK من GitHub Actions:
================================================================================

1. اذهب إلى:
   https://github.com/YOUR_USERNAME/jabalia-attendance-app/actions

2. اختر "Build Android APK" من القائمة

3. اختر آخر workflow نجح (علامة ✅ خضراء)

4. في أسفل الصفحة، قسم "Artifacts":
   - app-universal-release.apk.zip (موصى به للجميع)
   - app-arm64-v8a-release.apk.zip (أجهزة حديثة فقط)

5. حمّل الملف وفك الضغط

6. ثبّت ملف APK على الهاتف


🔧 إعدادات GitHub Actions:
================================================================================

إذا كان Build يفشل، تأكد من:

1. الملف موجود في المسار الصحيح:
   flutter_attendance_app/.github/workflows/build-apk.yml

2. الـ workflow مفعّل في Settings:
   Settings → Actions → General → Allow all actions

3. Permissions صحيحة:
   Settings → Actions → General → Workflow permissions
   → Read and write permissions


📱 تثبيت APK على الهاتف:
================================================================================

1. حمّل ملف APK (universal) من GitHub Actions

2. فك الضغط (ZIP file)

3. انقل ملف APK إلى الهاتف (USB/WhatsApp/Email)

4. افتح ملف APK على الهاتف

5. سيطلب "السماح بالتثبيت من مصادر غير معروفة":
   ✓ اضغط "الإعدادات"
   ✓ فعّل "السماح من هذا المصدر"
   ✓ ارجع واضغط "تثبيت"

6. قد يظهر "التطبيق غير آمن":
   ✓ اضغط "معلومات إضافية"
   ✓ اضغط "تثبيت على أي حال"

7. انتهى! افتح التطبيق


🧪 اختبار التطبيق:
================================================================================

بعد التثبيت، تأكد من:

✅ صفحة تسجيل الدخول تظهر بوضوح
✅ حقول الإدخال بيضاء وواضحة (ليست رمادية)
✅ يمكن كتابة اسم مستخدم وكلمة مرور
✅ زر "تسجيل الدخول" يعمل
✅ الألوان والأيقونات صحيحة


🔍 استكشاف الأخطاء:
================================================================================

مشكلة: "لا يمكن تثبيت التطبيق"
--------------------------------
الحل:
1. امسح الإصدار القديم أولاً
2. فعّل "مصادر غير معروفة"
3. تأكد من تنزيل ملف APK الصحيح (universal)

مشكلة: "GitHub Actions يفشل"
-----------------------------
الحل:
1. تحقق من Logs في Actions
2. تأكد من أن المسار صحيح
3. قد يحتاج تحديث Flutter version في workflow

مشكلة: "حجم الملف كبير جداً"
---------------------------
الحل:
1. استخدم APK المخصص للـ architecture:
   - arm64-v8a للأجهزة الحديثة (أصغر حجم)
2. أو استخدم split APKs بدلاً من universal

مشكلة: "التطبيق يتوقف عند الفتح"
-------------------------------
الحل:
1. تأكد من الاتصال بالإنترنت (أول مرة)
2. امسح بيانات التطبيق
3. أعد التثبيت


📋 الملفات المهمة في المشروع:
================================================================================

flutter_attendance_app/
├── lib/
│   ├── screens/
│   │   └── login_screen.dart    ✨ محدث v1.0.2
│   ├── config/
│   │   └── api_config.dart      (https://jabalia.ps/api)
│   └── main.dart
├── pubspec.yaml                 (version: 1.0.2+3)
├── .github/
│   └── workflows/
│       └── build-apk.yml        ✨ جديد
├── ANDROID-BUILD.md             ✨ جديد
└── HOW-TO-BUILD.txt             ✨ هذا الملف


💡 نصائح:
================================================================================

✅ استخدم universal APK إذا كنت غير متأكد من architecture الجهاز
✅ GitHub Actions مجاني للمستودعات العامة (2000 دقيقة/شهر)
✅ كل build يأخذ 5-7 دقائق تقريباً
✅ يمكنك تحميل APK من Artifacts لمدة 90 يوم
✅ لإصدار جديد، غيّر version في pubspec.yaml وارفع


🎯 الخلاصة:
================================================================================

1. التطبيق محدّث إلى v1.0.2 مع إصلاح مشكلة حقول الإدخال
2. استخدم GitHub Actions لبناء APK تلقائياً
3. حمّل universal APK من Artifacts
4. ثبّت على الهاتف واستمتع!


================================================================================
                       التطبيق جاهز للبناء! 🚀
================================================================================
